# ==================================================
# FRUITBOX REFERRAL MARKETING SYSTEM - ENVIRONMENT CONFIGURATION
# ==================================================

# ==================================================
# REQUIRED: DATABASE CONFIGURATION
# ==================================================
# PostgreSQL connection string - REQUIRED for the application to work
# Format: postgresql://username:password@host:port/database_name
# Examples:
#   - Replit Database: postgresql://username:password@db.replit.com:5432/main
#   - Neon Database: postgresql://username:password@ep-cool-moon-123456.us-east-1.aws.neon.tech/neondb
#   - Local Database: postgresql://postgres:password@localhost:5432/fruitbox
DATABASE_URL=postgresql://username:password@host:port/database_name

# ==================================================
# REQUIRED: SESSION SECURITY
# ==================================================
# Secure random string for session encryption - REQUIRED for authentication
# Generate with: openssl rand -hex 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Must be at least 32 characters long for security
SESSION_SECRET=your-secure-session-secret-minimum-32-characters-long

# ==================================================
# OPTIONAL: WHATSAPP INTEGRATION (INTERAKT)
# ==================================================
# Interakt API credentials for WhatsApp messaging
# Get these from: https://app.interakt.ai/
# If not provided, WhatsApp features will be disabled but app will still work
INTERAKT_API_TOKEN=your-interakt-api-token-from-dashboard
INTERAKT_BUSINESS_NUMBER=your-whatsapp-business-number-with-country-code

# ==================================================
# OPTIONAL: APPLICATION CONFIGURATION
# ==================================================
# Environment mode - affects logging and error handling
# Options: development, production
NODE_ENV=development

# Server port - only needed for local development
# In Replit, this is automatically configured
PORT=5000

# Debug logging - set to enable detailed logs
# DEBUG=*

# ==================================================
# DEPLOYMENT NOTES
# ==================================================
# For Replit:
#   - Set these in Replit Secrets (not .env file)
#   - DATABASE_URL is auto-provided if using Replit Database
#   - Generate SESSION_SECRET and add to Secrets
#
# For Vercel/Railway/Heroku:
#   - Set these in platform environment variables
#   - Ensure DATABASE_URL points to production database
#   - Use strong SESSION_SECRET in production
#
# For Local Development:
#   - Copy this file to .env
#   - Replace placeholder values with real credentials
#   - Never commit .env file to version control

# ==================================================
# SETUP VERIFICATION
# ==================================================
# After setting these variables:
# 1. Run: npm run db:push (to create database tables)
# 2. Run: npm run dev (to start the application)
# 3. Visit: http://localhost:5000 (or your Replit URL)
# 4. Create admin account and test basic functionality